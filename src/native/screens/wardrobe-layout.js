export const WARDROBE_CHIP_METRICS = Object.freeze({
  height: 34,
  minWidth: 64,
  maxWidth: 136,
  horizontalPadding: 14,
  borderRadius: 17,
  iconGap: 6,
  fontSize: 13,
  lineHeight: 16,
});

export const WARDROBE_GRID_METRICS = Object.freeze({
  gap: 12,
  columns: 2,
  cardWidth: '48%',
  cardMinWidth: 0,
});

export const WARDROBE_EMPTY_STATE_METRICS = Object.freeze({
  maxWidth: 320,
  iconSize: 48,
  actionMinWidth: 108,
});
